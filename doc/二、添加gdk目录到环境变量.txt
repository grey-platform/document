为了编译的方便，可以考虑把gdk目录设置到环境变量中去。
直接在环境变量中添加一个GDK_PATH的环境变量，并把它的值设置为gdk的目录。
如GDK_PATH=E:\grey_git\gdk
或直接执行行 E:\grey_git\gdk 目录下的“添加环境变量.bat” 
这样就可以把gdk目录设置为一个环境变量了，后面编译时方便使用。

注意：在运行bat文件时可能要用管理员身份来运行哈，要不然没有权限白玩。